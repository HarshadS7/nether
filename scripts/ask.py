"""
Nether — Q&A System

Combines vector search, graph queries, and LLM to answer questions
about the ingested codebase.
"""

# TODO: Phase 5 implementation
# - answer_question(question, collection, G) -> str
# - vector retrieval step
# - graph query step
# - build LLM prompt
# - call Ollama local LLM  (e.g. ollama.chat(model=OLLAMA_MODEL, ...))
# - format answer with sources
# - fallback: graph-only answers
