"""
Nether — Vector Store & Embeddings

Embeds code chunks into ChromaDB for semantic search.
"""

# TODO: Phase 4 implementation
# - init_vector_store() -> chromadb.Collection
# - chunk_files(parsed_files) -> list[dict]
# - embed_and_store(chunks, collection)
# - search(query, collection, top_k=5) -> list[dict]
# - clear_store(collection)
